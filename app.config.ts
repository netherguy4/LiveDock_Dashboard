import { defineAppConfig } from "nuxt/app";

export default defineAppConfig({
  branding: {
    title: "Monitoring",
    shortTitle: "Mon",
  },
  ui: {
    sidebarWidth: 248,
    sidebarCollapsedWidth: 64,
  },
});
