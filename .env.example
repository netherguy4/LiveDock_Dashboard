# UI login credentials (form-based).
LOGIN=admin
PASSWORD=replace-me

# HMAC secret used to sign session cookies. Generate with: openssl rand -hex 32
SESSION_SECRET=replace-me


# Nitro deploy preset (vercel by default).
# NITRO_PRESET=node-server
